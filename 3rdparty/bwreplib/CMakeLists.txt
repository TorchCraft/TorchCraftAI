IF(NOT MSVC)
  SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS}\
 -Wno-sign-compare\
 -Wno-unused-variable")
ENDIF(NOT MSVC)

FILE(GLOB SRCS *.cpp)
ADD_LIBRARY(bwreplib ${SRCS})
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})
SET_PROPERTY(TARGET bwreplib PROPERTY POSITION_INDEPENDENT_CODE ON)
